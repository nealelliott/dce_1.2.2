#
# The server here does nothing (so no args)
SERVER_ARGS=
# It does not write a binding file, so we shouldn't wait for it
NO_BINDING_FILE=yes
# The client only takes the number of passes
CLIENT_ARGS="\$REPEAT_COUNT"
