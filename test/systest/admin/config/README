

	Unconfigure a DCE client
	=======================

	The file DCPSAN004 found in this directory is a dcecp script
	that, when run, it unconfigures a DCE client machine.

	To run this script type:

		DCPSAN004 <client-hostname>

	This script must be run on the DCE server host. After having
	run DCPSAN004 successfully, the script

		/etc/dce.rm

	must be run on the DCE client before reconfiguring.

