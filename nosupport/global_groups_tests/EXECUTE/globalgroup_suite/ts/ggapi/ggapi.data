#!/bin/ksh
#
# 

TMPDIR=${TMPDIR:-/tmp}                  # Temporary working directory

GG_INPUT="${TMPDIR}/gg_input_$$"	# Input file used to execute batch mode
GG_OUTPUT="${TMPDIR}/gg_output_$$"	# Output file used to execute batch mode

DO_GGAPI="./do_ggapi"			# Executable for executing APIs

RGY_EXEC="rgy_edit -update"		# Executable for editing registry.
DCECP_EXEC="dcecp"			# Executable for editing registry.
