# This is for daemon.dist.scr test.
# Test only the bkg daemon from one machine and it is DFS only test.
"" /tmp/file_test /:/wd cell_admin -dce- daemon.dist.scr.1.log
"" /tmp/file_test /:/wd cell_admin -dce- daemon.dist.scr.2.log
"" /tmp/file_test /:/wd cell_admin -dce- daemon.dist.scr.3.log
