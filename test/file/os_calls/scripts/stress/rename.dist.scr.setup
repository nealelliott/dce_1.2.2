# This setup file is for rename.dist.scr. You have to have at least five spokes.
# You may want to exercise the combinations of dfs and local path;
# Change any spok's working dir to local path. 
# You may want some local path too.
"" /tmp/file_test /:/wd cell_admin -dce- rename.dist.scr.1.log
"" /tmp/file_test /:/wd cell_admin -dce- rename.dist.scr.2.log
"" /tmp/file_test /:/wd cell_admin -dce- rename.dist.scr.3.log
"" /tmp/file_test /:/wd cell_admin -dce- rename.dist.scr.4.log
"" /tmp/file_test /:/wd cell_admin -dce- rename.dist.scr.5.log
