# Need n concurent test processes on same DFS client. Need at least 4 spokes. 
# if we setup 8 spokes there may be an error pthread create eerror=12 no
# memory space. Basically this test needs to be done in one machine.
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.1.log
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.2.log
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.3.log
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.4.log
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.5.log
"" /tmp/file_test /:/wd cell_admin -dce- name.resolution.basic.scr.6.log
