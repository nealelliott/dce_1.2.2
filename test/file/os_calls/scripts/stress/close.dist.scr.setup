# This setup file is used by close.dist.scr test.
# NOTE:You have to run the test in kind of order, say the first spoke 
#      should be running within the same machine that has exported the testdir.
#      The HUB must be running within the machine metioned above.
"" /tmp/file_test /:/wd cell_admin -dce- close.dist.scr.1.log
"" /tmp/file_test /:/wd cell_admin -dce- close.dist.scr.2.log
