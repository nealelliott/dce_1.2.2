READ ME FIRST
=============

This directory contains all functional tests for the
generic ERA interfaces. However currently there is
only test for the generic attribute schema interface.

Before you start to run the tests, PLEASE read tetexec.cfg 
file and make necessary changes for your test environment.
The tetexec.cfg should contain all of information you may 
need to make the changes, however TET manual can give the 
details you may ask.

To start tests, do -):

Step 1: set renvironment variable TET_ROOT

Step 2: For all tests, 
		tcc -e functional/dce/utils/attr
	For ERA schema, 
		tcc -e functional/dce/utils/attr xattrschema
	

